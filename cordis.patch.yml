- id: permission
  config:
    presets:
      read-only:
        sandbox: read-only
        approval: ask
      workspace-write:
        sandbox: workspace-write
        approval: ask
      auto:
        sandbox: workspace-write
        approval: ask
        name: Auto
        description: Fresh model risk and authorization review. Exact manual file edits; unisolated execution and sandbox widening are blocked.
      danger-full-access:
        sandbox: danger-full-access
        approval: never

- insert:
    - id: auto-permission-mode
      name: '@nanmicoder/dsh-auto-mode'
      config: {}
